'use strict';

System.register([], function (_export, _context) {
  var gridFilter;
  return {
    setters: [],
    execute: function () {
      _export('gridFilter', gridFilter = {
        input: 'input',
        dateFromTo: 'date-from-to',
        boolean: 'boolean',
        select: 'select'
      });

      _export('gridFilter', gridFilter);
    }
  };
});