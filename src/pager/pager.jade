template
  ul.pagination
    li.page-item(if.bind="showFirstLastButtons", class="${ prevDisabled ? 'disabled' : '' }")
      a.page-link(href="", click.delegate="first()", aria-label="First")
        span(aria-hidden="true") First

    li.page-item(if.bind="showJumpButtons", class="${ prevDisabled ? 'disabled' : '' }")
      a.page-link(href="", click.delegate="prevJump()", aria-label="Next")
        span(aria-hidden="true") &#8230;

    li.page-item(class="${ prevDisabled ? 'disabled' : '' }")
      a.page-link(href="", click.delegate="prev()", aria-label="Previous")
        span(aria-hidden="true") &laquo;

    li.page-item(repeat.for="curPage of pages", class="${ curPage == $parent.page ? 'active' : '' }")
      // This span is an IE bug fix - if a &raquo; or many other chars are used before an interpolation for some reason that interpolation fails, following ones are fine (this is for the same property). Adding an invisible span with an interpolate in it fixes the issue
      span(style="display:none") ${curPage}
      a.page-link(href="", click.delegate="$parent.changePage(curPage)") ${curPage}

    li.page-item(class="${ nextDisabled ? 'disabled' : '' }")
      a.page-link(href="", click.delegate="next()", aria-label="Next")
        span(aria-hidden="true") &raquo;

    li.page-item(if.bind="showJumpButtons", class="${ nextDisabled ? 'disabled' : '' }")
      a.page-link(href="", click.delegate="nextJump()", aria-label="Next")
        span(aria-hidden="true") &#8230;

    li.page-item(if.bind="showFirstLastButtons", class="${ nextDisabled ? 'disabled' : '' }")
      a.page-link(href="", click.delegate="last()", aria-label="Last")
        span(aria-hidden="true") Last
